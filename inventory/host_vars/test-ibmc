# Specific variables for this server
baremetal_name: "ibmc-01"
baremetal_ibmc_addr: "ibmc 接口地址，例如: https://<ip address>"
baremetal_ibmc_user: "ibmc 接口用户名"
baremetal_ibmc_pass: "ibmc 接口密码"
baremetal_ibmc_sys: "实例对应的裸机资源地址，例如: /redfish/v1/Systems/1"
baremetal_ibmc_verify_ca: "False"
baremetal_fqdn: "test-ibmc.example.com"
baremetal_macs: ["裸机MAC地址1", "裸机MAC地址2"]
# 裸机镜像OS，这里默认安装Ubuntu xenial, 相关配置在 vars/test/images/<name>.yml
baremetal_os: "xenial"
# If you change the interface name to eth1 (for example)
# Remember to change the backend devices!
# 裸机网络配置
baremetal_network:
  - device: "eno1"
    address: "10.0.0.160"
    netmask: "255.255.255.0"
    gateway: "10.0.0.1"
    nameservers: ["8.8.8.8"]
    domain: "example.com"

# The rest of the variables for Ironic are not defined here, they are
# defined on the inventory, you can define more variables here
# for other playbooks
